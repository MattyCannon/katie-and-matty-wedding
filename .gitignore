# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files (RSVP / Google Sheets credentials go here later — never commit)
.env
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
.env*
!.env.local.example

# Source artwork — licensed Adobe Stock original + stock photos. Kept local:
# stock licences allow using an asset in the finished site (see the derived
# cut-outs in public/wildflowers/) but not redistributing the original file.
/imgs/
