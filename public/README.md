# public/

Static assets served from the site root.

Drop files here and reference them with a leading slash:

- `public/invite.jpg` → available at `/invite.jpg`
- `public/favicon.ico` → `/favicon.ico`

**Tip:** add the paper invitation here (e.g. `invite.jpg` or `invite.png`) so the
botanical illustrations can be tuned to match it.
